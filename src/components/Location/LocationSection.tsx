import { business, openingHours } from "../../data/business"; import { createWhatsAppUrl } from "../../utils/whatsapp";
export default function LocationSection() { const today = new Date().getDay(); return <section className="section content-shell" id="localizacao" aria-labelledby="location-title"><header className="section-heading"><p className="eyebrow">VENHA NOS VISITAR</p><h2 id="location-title">Localização</h2></header><div className="location-card"><div className="location-address"><span aria-hidden="true">⌖</span><div><strong>{business.address.street}</strong><p>{business.address.district}<br />{business.address.city} — {business.address.state}</p><a href={`tel:+${business.phoneNumber}`}>{business.phoneDisplay}</a><br /><a href={business.instagramUrl} target="_blank" rel="noopener noreferrer">{business.instagramUsername}</a></div></div><div className="location-actions"><a className="secondary-button" href={business.mapsUrl} target="_blank" rel="noopener noreferrer">Abrir no Google Maps</a><a className="ghost-button" href={createWhatsAppUrl(business.phoneNumber, business.bookingMessage)} target="_blank" rel="noopener noreferrer">Chamar no WhatsApp</a></div><div className="hours" aria-label="Horários de funcionamento">{openingHours.map((item, index) => <div className={index === today ? "today" : ""} key={item.day}><span>{item.day}{index === today && <small> hoje</small>}</span><b>{item.hours}</b></div>)}</div></div></section>; }
